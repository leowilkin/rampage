{
  "name": "rampage",
  "version": "1.0.0",
  "description": "pre-commit hook that scans staged changes for PII with NDS Rampart",
  "type": "module",
  "scripts": {
    "prepare": "git config core.hooksPath hooks",
    "scan": "node scan-staged.mjs",
    "deploy": "cp setup.sh scan-staged.mjs public/ && wrangler deploy"
  },
  "license": "ISC",
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@nationaldesignstudio/rampart": "^0.1.3"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
